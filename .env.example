# Server Configuration
PORT=3001

# Authentication (generate a strong random token)
# Example: openssl rand -hex 32
AUTH_TOKEN=your_secret_token_here

# Printer Configuration
# Options: 'usb' or 'network'
PRINTER_TYPE=usb

# For network printers only:
# PRINTER_IP=192.168.1.100
# PRINTER_PORT=9100

# Email Printing (Optional - leave blank to disable)
# Get your API key from: https://resend.com/api-keys
RESEND_API_KEY=
# Webhook signing secret from Resend webhook settings
RESEND_WEBHOOK_SECRET=

# Image Moderation (Optional but recommended)
# Get your API key from: https://platform.openai.com/api-keys
# Without this, image content won't be moderated (security risk!)
OPENAI_API_KEY=
